---
name: github-sync
description: >
  Git operations via GitHub API — commit, push, pull, create repos, sync files between Cowork
  and GitHub. Use this skill whenever the user mentions git, GitHub, push, pull, commit, repo,
  repository, sync code, backup to GitHub, version control, or wants to upload/download files
  to/from a GitHub repository. Also trigger when someone says "push this to GitHub",
  "sync my project", "create a repo", "commit my changes", "pull latest", or references
  any filipvaleriu/* repository. Works from Cowork sandbox via GitHub REST API through
  the browser (Claude in Chrome), bypassing sandbox network restrictions.
---